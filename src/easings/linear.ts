export default linear;


// velocity over time
// as time increases velocity increases 
// both start at 0
// increase along duration and to

function linear(from: number, to: number, durationInMS: number) {
  let _from = from;
  let _to = to;
  let _duration = durationInMS;

  if(_from === _to) {
    return;
  }
}

